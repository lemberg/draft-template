# -*- mode: ruby -*-
# vi: set ft=ruby :

# Define project base path.
PROJECT_BASE_PATH = File.dirname(File.expand_path(__FILE__))

# Load Draft Environment main Vagrantfile.
load File.dirname(__FILE__) + "/vendor/lemberg/draft-environment/Vagrantfile"
